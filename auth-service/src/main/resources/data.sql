-- auth-service uses its own table instead of the customer table.
-- For this beginner-friendly version we keep registration dynamic,
-- so the main demo user is usually created through POST /auth/register.
SELECT 1;
